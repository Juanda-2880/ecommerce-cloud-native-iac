# This file previously contained IAM role creation logic.
# In AWS Academy Sandbox, we use pre-existing instance profiles.
# The 'instance_profile_name' variable is now centrally managed in variables.tf
