FROM philippaul/node-mysql-app:02

# The image philippaul/node-mysql-app:02 already contains the application.
# Environment variables (DB_HOST, DB_USER, DB_PASS) should be passed at runtime.

EXPOSE 3000
